machine:
  node:
    version: 8.1.2

dependencies:
  cache_directories:
    - "bower_components"

deployment:
  release:
    tag: /^v?\d+\.\d+\.\d+(?:-beta\.\d+)?$/
    owner: Financial-Times
    commands:
      - make npm-publish
