machine:
  node:
    version: 6.6.0

dependencies:
  cache_directories:
    - "bower_components"

deployment:
  release:
    tag: /^v[0-9]+(.[0-9]+)*/
    owner: Financial-Times
    commands:
      - make npm-publish
